Dwie wersje tego samego quiz'u.

Jedna wersja do zabawy w lini komend:
`python quiz.py` lub `ipython quiz.py`

Wersja druga, najpierw wymaga zainstalowania modułu Flask:
`conda install -c anaconda flask`
a potem podobnie:
`python quiz_web.py`

